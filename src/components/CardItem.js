import React from 'react'
import { CardContent, dividerClasses, makeStyles } from '@mui/material';
import Typography from '@mui/material';
import {red} from '@mui/material';







export default function CardItem({id,nombre,img,birthdate,description}){


return (
    <div> Personajes</div>
)


}